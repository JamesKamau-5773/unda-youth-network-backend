# ============================================
# UNDA YOUTH NETWORK - ENVIRONMENT VARIABLES
# ============================================
# CRITICAL: Never commit the actual .env file to git!
# This .env.example file shows required variables.
# Copy this to .env and fill in real values.

# ============================================
# SECURITY - REQUIRED FOR ALL ENVIRONMENTS
# ============================================
# Generate strong secret key with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=your-secret-key-here-change-this-to-a-random-64-character-string

# ============================================
# DATABASE - REQUIRED
# ============================================
# Development example:
# DATABASE_URL=postgresql://unda_user:unda_password@localhost/unda_db
#
# Production example (Railway, Render, Heroku):
# DATABASE_URL=postgresql://user:pass@host.provider.com:5432/db_name
#
# Note: Some providers use postgres:// (change to postgresql://)
DATABASE_URL=postgresql://username:password@localhost/unda_db

# ============================================
# REDIS - REQUIRED FOR RATE LIMITING
# ============================================
# Development:
# REDIS_URL=redis://localhost:6379
#
# Production (Railway, Render, Upstash):
# REDIS_URL=redis://default:password@host.provider.com:6379
REDIS_URL=redis://localhost:6379

# ============================================
# ENVIRONMENT - REQUIRED
# ============================================
# Options: development, production
# IMPORTANT: Set to "production" on live server
FLASK_ENV=development

# ============================================
# PRODUCTION-ONLY SETTINGS
# ============================================
# When FLASK_ENV=production, these are automatically enabled:
# - HTTPS-only cookies (SESSION_COOKIE_SECURE=True)
# - HSTS security headers
# - Secure session settings
# - No debug mode

# ============================================
# OPTIONAL: GUNICORN CONFIGURATION (Production)
# ============================================
# Number of worker processes (CPU cores * 2 + 1)
# WORKERS=4
# Bind address (0.0.0.0 for public access)
# BIND=0.0.0.0:5000
# Timeout for requests (seconds)
# TIMEOUT=120

# ============================================
# SETUP INSTRUCTIONS
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Generate SECRET_KEY: python -c "import secrets; print(secrets.token_hex(32))"
# 3. Update DATABASE_URL with your PostgreSQL credentials
# 4. Update REDIS_URL with your Redis credentials
# 5. For production: Set FLASK_ENV=production
# 6. NEVER commit .env to git (already in .gitignore)

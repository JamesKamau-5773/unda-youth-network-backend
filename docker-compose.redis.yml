version: '3.8'
services:
  redis:
    image: "redis:7-alpine"
    container_name: unda_redis_dev
    ports:
      - "6379:6379"
    restart: unless-stopped
    volumes:
      - redis-data:/data

volumes:
  redis-data:
